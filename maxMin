def maxMin(k, arr):
    arr.sort()
    val = float("inf")
    for i in range(len(arr)-k+1):
        unfair = arr[i+k-1] - arr[i]
        #if you try splitting list like x = arr[i:i+k], it causes timeout
        if unfair < val:
            val = unfair
    return val
